/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["images.pexels.com", "another-domain.com", "third-domain.net"],
    },
  };
  
  export default nextConfig;
  